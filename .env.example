# URL of the deployed AI proxy worker (see server/README.md).
AI_PROXY_URL=https://ironlog-ai-proxy.<your-subdomain>.workers.dev

# Optional. Must match APP_SHARED_SECRET on the worker.
# This value ships inside the app bundle — it deters casual reuse, nothing more.
AI_APP_KEY=
